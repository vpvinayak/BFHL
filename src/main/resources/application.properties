# Configurable server port with environment variable fallback for hosting (Render, Railway, etc.)
server.port=${PORT:8080}

# Default details for user response (Update with your own details before submission)
app.user.id=john_doe_17091999
app.user.email=john@xyz.com
app.user.roll-number=ABCD123
